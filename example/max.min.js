const numbers = [10,4,8,6,9,2,-12,5,15];

let maximum = -Infinity
let minimum = Infinity

for(let number of numbers){
    
    if(number > maximum)
    maximum = number;

    if(number < minimum)
    minimum = number;
    
}

console.log(maximum);
console.log(minimum);
